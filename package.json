{
  "name": "osu-imagemap-builder",
  "version": "2.0.0",
  "private": true,
  "description": "A visual, browser-based osu! imagemap BBCode builder.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "verify:build": "node scripts/verify-build.mjs",
    "check": "npm run test && npm run build && npm run verify:build"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
